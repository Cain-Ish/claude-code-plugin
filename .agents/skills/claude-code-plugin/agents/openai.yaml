interface:
  display_name: "Claude Code Plugin"
  short_description: "Repo-specific patterns and workflows for claude-code-plugin"
  default_prompt: "Use the claude-code-plugin repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true