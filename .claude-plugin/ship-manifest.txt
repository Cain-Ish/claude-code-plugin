# ship-manifest — the ONLY files a marketplace install receives (docs/plans/2026-09-23-ship-manifest.md).
# scripts/build-plugin.sh assembles plugin/ from this list; tests/test-plugin-dist-current.sh fails
# when plugin/ drifts from it. Everything not listed (tests/, docs/, mcp/src/, .claude/, .superpowers/,
# .codex/, CONSTITUTION.md, RELEASING.md, …) stays in the dev repo. Marketplace source: ./plugin.
#
# Line forms:   path/to/file        one file
#               dir/                every file under dir (recursive)
#               dir/ *.pattern      files under dir matching -name pattern (recursive)
# Always skipped: *.stackdump, .DS_Store, Thumbs.db, node_modules/, *.tmp.*
.claude-plugin/plugin.json
.claude-plugin/mcp.json
hooks/hooks.json
skills/
agents/
output-styles/
scripts/
bin/
mcp/dist/ *.bundle.js
mcp/package.json
mcp/package-lock.json
kb-schema.json
model-ladder.json
systemd/
LICENSE
NOTICE.md
