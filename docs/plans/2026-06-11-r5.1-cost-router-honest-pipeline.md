# R5.1 — Cost-Router Honest Pipeline + Install — Implementation Plan

> **For agentic workers:** Implement task-by-task following TDD. See `second-brain:test-driven-development` and `second-brain:verification-before-completion`.

**Goal:** Make cost-router's data pipeline honest (events actually log, no fabricated budget lines, high-precision nudges), end the generated-page churn, close the R1-deferred nested-spawn gap, install the plugin, and start the 2-week keep-or-kill dogfood clock.

**Architecture:** All S-effort fixes from the audit (CR-002/006/008/009/010/011 + the CR-007/SCRIPTS-06/MCP-CHURN-1 churn class via a shared `sb_write_generated_page` lib.sh helper). The M-effort re-denomination (CR-003/004/005: opus_calls framing, Fable tier, setup trade-off) is DEFERRED to the dogfood verdict per the spec. Orchestrate Step 7 stops printing a fabricated budget line now (minimal honesty slice of CR-003).

**Versioning:** cost-router `0.1.1 → 0.1.2`; second-brain `0.24.39 → 0.24.40` (lib.sh helper + capture script + migration row); marketplace lockstep for BOTH entries (validate-plugin only gates `.plugins[0]` — R8 fixes that; check cost-router manually).

**Evidence:** findings CR-001..011, SCRIPTS-06, MCP-CHURN-1 in `docs/specs/2026-06-10-plugin-deep-dive-findings-appendix.md`; R1 deferral (cost-router hooks ignore SB_NESTED_SPAWN) in PR #52.

---

### Task 1: Branch
`git checkout -b fix/0.24.40-r5.1-cost-router` from updated main; commit this plan.

### Task 2: route-log empty-models-CSV fix (CR-002)
- Test (RED) in `tests/test-route-log.sh`: `route-log.sh emit t DO "" 0 false classified false` must append a line with `"models":[]`.
- Fix `cost-router/scripts/route-log.sh:63-64`: empty CSV short-circuits to `[]` (the `printf '' | jq -Rc 'split(",")'` pipe exits 0 with NO output, so the `||` fallback never fires and `--argjson models ""` kills the whole emit).

### Task 3: classifier precision (CR-006)
- Tests (RED) in `tests/test-classify-prompt.sh`: `continue` → NO output; `yes` → NO output; `implement step 3 of the plan in docs/plans/foo.md` → NO nudge (DO suppressed; `*plan*` substring removed); `fix the lint warning in the security module` → SCOUT (lint) not THINK (`*security*` dropped); `design the auth architecture for the new service` → THINK nudge; `find the config loader` → SCOUT nudge; ALL prompts still log a route-log event (learning data) regardless of nudging.
- Fix `cost-router/scripts/classify-prompt.sh`: word-boundary matching via padded string (`P_PAD=" $P_LOWER "` and `*" design"*`-style patterns); THINK list drops `refactor` and `security`, `plan` becomes word-bounded; nudge emitted ONLY for THINK/SCOUT and prompts ≥ 25 chars; DO is silent; rl_emit always runs.

### Task 4: pure-read budget + clamped banner (CR-009)
- Tests (RED) in `tests/test-opus-budget.sh`: `spent` on a stale-dated ledger returns 0 WITHOUT rewriting the file (content unchanged). In a new `routing-status` assertion (same file or test-cost-router-validate style): over-cap renders `over cap`, never `$-0.50 remaining`.
- Fix `cost-router/scripts/opus-budget.sh` (`ob_today_spent`): stale date → `printf '0'`, no `_ob_reset` (reset stays lazy in `ob_record`). Fix `cost-router/scripts/routing-status.sh`: clamp negative remaining to `over cap`.

### Task 5: orchestrate honesty (CR-008 + CR-003 slice)
`cost-router/skills/orchestrate/SKILL.md`: add `subagent_type: 'cr-planner'|'cr-implementer'|'cr-scout'` to the four Task examples (the prose says dispatch cr-*, the copyable examples spawn general-purpose agents); replace the fabricated Step 7 line `Opus budget used: $0.18 / $5.00 today` with an instruction to print the REAL ledger (`bash "${CLAUDE_PLUGIN_ROOT}/scripts/opus-budget.sh" spent` → "Opus ledger: $<spent> recorded today" or "no Opus spend recorded (ledger is written by persona_think; cost-router dispatches are not yet metered)").

### Task 6: setup grants (CR-011)
`cost-router/skills/setup/SKILL.md` frontmatter: drop `Bash(rm *)` (used nowhere in the flow); keep `Bash(mv *)` (the settings-file write). `tests/test-cost-router-validate.sh` gets an assertion: setup's allowed-tools must NOT contain `Bash(rm`.

### Task 7: README honesty (CR-010)
`cost-router/README.md`: replace the unverifiable "Opus spend drops ~70–85%" with the price-ratio statement (Sonnet $3/$15 is 60% of Opus $5/$25 per token; Haiku $1/$5 is 20% — savings proportional to how much work routes down-tier; measure your own delta after a week of route-log data); rephrase "learns which routing choices paid off" as accumulating-the-data-to-learn (conditional until the loop is proven).

### Task 8: nested-spawn guard for cost-router hooks (R1 deferral)
- Test (RED): extend `tests/test-nested-spawn-guard.sh` with Part F — `cost-router/scripts/classify-prompt.sh` and `cost-router/scripts/routing-status.sh` carry the guard line AND no-op at runtime under `SB_NESTED_SPAWN=1`.
- Fix: insert the standard 2-line guard after `set -u` in both.

### Task 9: generated-page contract (CR-007 / SCRIPTS-06 / MCP-CHURN-1)
- Tests (RED): `tests/test-cost-router-capture.sh` asserts the output page (a) lives at `wiki/state/cost-routing-patterns.md`, (b) starts with valid frontmatter (`title:`, `description:`, `type: state`, `generated: true`), (c) survives a `knowledge_validate`-style frontmatter check (has the `---` fence). New `tests/test-lib-generated-page.sh`: `sb_write_generated_page <path> <title> <desc>` writes frontmatter + marker `<!-- generated: do not hand-edit -->` then body from stdin, atomically.
- Fix: new lib.sh helper `sb_write_generated_page`; `scripts/cost-router-capture.sh` routes its writer through it and targets `wiki/state/`; any in-repo readers of the old root path (grep `cost-routing-patterns`) updated; migration row instructs deleting the stale root-level page.

### Task 10: versions + migration row + full verification
cost-router plugin.json + marketplace entry → 0.1.2; second-brain plugin.json + marketplace → 0.24.40; migration row (mentions the root-page relocation: `rm ~/knowledge/wiki/cost-routing-patterns.md` precondition-free cleanup the capture hook re-creates under wiki/state/). `bash tests/run-all.sh` ALL GREEN; `bash scripts/validate-plugin.sh` OK; verify cost-router lockstep manually (`jq` both files).

### Task 11: gate + PR + install + dogfood clock
Deep review (focused: cost-router unit + history + premise), fix findings, push, PR, merge. Then `claude plugin install cost-router@second-brain` (verify with `claude plugin list` / installed_plugins.json), and pin the dogfood decision to PROJECT.md: deadline 2026-06-25 — keep (route-log shows nudges preceding model choices → invest CR-003/004/005) or kill (remove from marketplace + delete consumer hook + cr-* agents).

## Self-review
Coverage: CR-002→T2, CR-006→T3, CR-009→T4, CR-008+CR-003-slice→T5, CR-011→T6, CR-010→T7, R1-deferral→T8, CR-007/SCRIPTS-06/MCP-CHURN-1→T9, CR-001→T11. Deferred by design: CR-003/004/005 full re-denomination (dogfood-gated), CR-005 Fable tier (same).
