{
  "hooks": {
    "SessionStart": [
      {
        "_comment": "matcher EXCLUDES 'compact' — upstream anthropics/claude-code#15174: SessionStart hook output is silently dropped after compaction (v2.0.72+), so running session-load.sh on the compact event was pure waste. Removing it stops the post-compact context-bloat loop reported by users on long sessions.",
        "matcher": "startup|resume|clear",
        "hooks": [
          { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/ensure-dirs.sh", "timeout": 5 },
          { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/discover-tools.sh", "timeout": 10 },
          { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/discover-installed.sh", "timeout": 10 },
          { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/discover-doc-sources.sh", "timeout": 10 },
          { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/session-load.sh", "timeout": 15 },
          { "_comment": "v0.12.0 dream auto-stage — runs after session-load.sh. When >= SB_DREAM_NEW_THRESHOLD (default 10) new transcripts have landed since the most-recent dream, stages a pending dream and emits a banner instructing Claude to spawn the dream-runner agent. Acceptance stays manual. Kill switch SB_DREAM_AUTOSTAGE=off (then session-load.sh shows the legacy session-count nag instead). Timeout 20s covers dream-snapshot.sh's wiki copy.",
            "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/dream-autostage.sh", "timeout": 20 }
        ]
      }
    ],
    "UserPromptSubmit": [
      { "hooks": [
        { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/persona-context.sh", "timeout": 10 }
      ]}
    ],
    "Stop": [
      { "hooks": [
        { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/stop-verify-gate.sh", "timeout": 10 },
        { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/stop-extract.sh", "timeout": 45 },
        { "_comment": "v2.10.0 SAR summary — aggregates this session's audit-log verdicts into a one-line Safety Adherence Rate banner via systemMessage. Read-only; runs last so it can't be preempted by the gate or extractor. Kill switch SB_SAR_SUMMARY=off.",
          "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/sar-summary.sh", "timeout": 5 }
      ]}
    ],
    "PreCompact": [
      { "matcher": ".*", "hooks": [
        { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/pre-compact.sh", "timeout": 45 }
      ]}
    ],
    "PreToolUse": [
      { "_comment": "v2.10.0 matcher broadened to cover Read (closes resource-scope gap where Read was declared in tool list but matcher didn't include it) and WebFetch/WebSearch/Task/MultiEdit (network + subagent + multi-file surface where tool_scope and sar_tool apply). MCP tools and read-only Glob/Grep/TodoWrite/BashOutput omitted to keep hook cost low.",
        "matcher": "Bash|Write|Edit|MultiEdit|Read|WebFetch|WebSearch|Task", "hooks": [
        { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/persona-tool-guard.sh", "timeout": 5 }
      ]},
      { "matcher": "Write|Edit|MultiEdit", "hooks": [
        { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/wiki-write-guard.sh", "timeout": 5 }
      ]},
      { "_comment": "v2.10.0 outbound info-flow guard (HarnessAudit sar_flow channel). Asks when an egress tool call (Bash with network tool, or WebFetch/WebSearch) carries credential-shaped content. Defense-in-depth against the agent helpfully pasting session secrets into a curl request. Kill switch SB_FLOW_GUARD=off.",
        "matcher": "Bash|WebFetch|WebSearch", "hooks": [
        { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/flow-guard.sh", "timeout": 5 }
      ]}
    ],
    "PostToolUse": [
      { "matcher": "Write|Edit", "hooks": [
        { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/quality-gate.sh", "timeout": 5 }
      ]},
      { "_comment": "v2.9.0 HarnessAudit Layer 3 — scans Read/WebFetch/Bash/Grep/Glob output for indirect-prompt-injection patterns. Flags via additionalContext; never blocks. Kill switch SB_INJECTION_SCAN=off.",
        "matcher": "Read|WebFetch|Bash|Grep|Glob", "hooks": [
        { "type": "command", "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/tool-return-scanner.sh", "timeout": 5 }
      ]}
    ]
  }
}
