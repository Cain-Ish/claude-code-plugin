---
title: Drain timeout now 120 seconds
description: the drain timeout is now one hundred twenty seconds
---
<!-- ai:begin (authored — flat YAML, see ai-block schema) -->
claim: the drain timeout is now one hundred twenty seconds
status: active
<!-- ai:end -->

The drain now uses a timeout of one hundred twenty seconds per transcript.
