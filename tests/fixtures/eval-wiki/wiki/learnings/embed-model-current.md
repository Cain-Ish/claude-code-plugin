---
title: Embedding model now bge-small
description: we now use the bge-small embedding model
---
<!-- ai:begin (authored — flat YAML, see ai-block schema) -->
claim: the embedding model is now bge-small
status: active
<!-- ai:end -->

We now use bge-small as the embedding model for all new vectors.
